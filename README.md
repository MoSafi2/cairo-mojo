In progress binding to libcairo. 
currently Under development and bugs can be expected. 
to see a working example check `examples/red_rectangle_png.mojo`.

Low-level binding can be found in `src/_cairo_binding.mojo` and they are mostly working.
Higher level API and mojo wrappers are in src/cairo.mojo but they are currently broken likely due to origin tracking.

